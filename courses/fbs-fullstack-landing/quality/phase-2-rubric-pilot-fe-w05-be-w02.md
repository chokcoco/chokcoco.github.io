# 第二阶段语义质检：FE-W05 与 BE-W02 标杆章

- 量规：通用课程评价量规 v1.0
- 审校范围：课程首页、术语表、FE-W05、BE-W02
- 第一阶段内容哈希：`472669d5ce21533eedeb3b9242af903b891a24a5d5eb7e5de57074fd25f2ef44`
- 第一阶段结果：0 error，0 warning
- 审校日期：2026-07-16

## 阻塞项扫描

未发现 blocker。两章覆盖蓝图指定学习成果；关键技术断言可由当前锁定仓库中的 route、wrapper、DTO、handler、test 和三个前端请求封装核验；缩减代码均标明边界；中文正文已完成 Humanizer 审查且未破坏术语或代码语义。

## 评分

| 维度 | 等级 | 权重分 | 置信度 | 通过证据 | 问题证据 |
| --- | ---: | ---: | --- | --- | --- |
| 学习目标与覆盖 | 4/4 | 20/20 | 高 | FE-W05 从 API 函数覆盖到 wrapper、代理、四层失败、三仓差异和契约练习；BE-W02 覆盖 route、wrapper、绑定、DTO、handler、灰度、响应和测试 | 两章是标杆范围，不覆盖 application/repository 的完整数据库实现；该内容按蓝图留给 BE-W03/BE-W04 |
| 事实与证据 | 4/4 | 25/25 | 高 | method/path、`Fbs-Request-Id`、Axios 0.18/1.12.2、Chassis r.13、`ScControllerWrapper`、`HandleParam`、`SellerSku`、GreySrv、HTTP 200 error envelope 均与当前代码吻合 | 课程没有启动公司环境做真实 HTTP 请求；正文已明确区分源码证据、unit test 与受控环境联调证据 |
| 受众适配与脚手架 | 4/4 | 15/15 | 高 | FE 章从后端熟悉的 HTTP 概念进入前端分层；BE 章从前一章的 Network 记录进入 Go 控制流；术语表覆盖跨端陌生词 | BE wrapper 的外部错误类型较多，正文只列名称并收束到统一映射，读者仍需在实际需求中按需深挖 |
| 解释深度与连贯性 | 4/4 | 15/15 | 高 | 两章共享一个接口，前端最终 path 与后端 route 对齐；机制、反例、诊断树、时序和边界形成连续推理 | FE 章的宿主代理不展开具体平台配置，这是有意遵守“只讲开发 Landing、旧 MMF 材料仅参考”的范围 |
| 练习与反馈 | 4/4 | 15/15 | 高 | 两章共用 `seller_sku`，分别给出归一化/请求矩阵与 handler/fake service/旧新链验证；均有可判定完成标准 | 未提供可直接写入 test 环境的账号与数据，练习把环境联调标为受控证据而非自动完成项 |
| 语言与教师感 | 4/4 | 10/10 | 高 | 两章 Humanizer 均为 46/50，语气具体克制，文件与症状替代宣传式概括 | 边界提醒中“不要”出现较多；它们都关联明确失败后果，暂不构成机械口吻 |
| **总分** |  | **100/100** |  |  |  |

## Humanizer

- FE-W05：46/50
- BE-W02：46/50
- 首页与术语表：47/50
- 结论：通过 45/50 门槛

详细记录见 `quality/humanizer-pilot-fe-w05-be-w02.md`。

## 问题清单

### Blocker

无。

### Major

无。

### Minor

1. 尚未在真实公司环境执行完整 HTTP 请求；浏览器阶段只验证静态课程交互、链接、布局和 Mermaid。正文已经把真实联调列为学员完成证据，没有伪报执行结果。
2. 标杆课程首页当前只展示两章，不等同于完整 40 章目录；首页已明确标注标杆状态，避免误导。
3. `build-static-course.mjs` 现在支持 `lesson.code` 与课程自定义首页元数据；需在后续其他静态课程构建时保留向后兼容检查。

## 定向建议

- 浏览器阶段重点检查两张 Mermaid 图、长表格、移动端侧栏和长代码块横向滚动。
- 后续生成 BE-W03/BE-W04 时复用本章控制流入口，不重复解释 wrapper 全部细节，转向 application/repository 与数据库证据。
- 后续生成其他前端章时复用四层失败模型，但必须换成该章真实链路，避免模板化复述。

## 决策

`ready`

总分不低于 85，六个维度均为 4，无 blocker/major，Humanizer 不低于 45。第三阶段体验检查已完成；其修复后已重新生成并重跑第一阶段，本报告引用最终内容哈希。
