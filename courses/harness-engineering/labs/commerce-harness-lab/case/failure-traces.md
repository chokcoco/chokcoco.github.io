# 失败轨迹

这些轨迹是虚构的，故意保留了现实项目中常见的信号缺口。

## 轨迹 A：价格看起来对，订单却不对

```text
10:04 任务：让会员折扣和满减券可以叠加。
10:07 智能体搜索 discount，命中 83 个文件。
10:12 智能体修改 apps/storefront/src/legacyPrice.js。
10:15 npm run lint 返回 0。
10:16 智能体报告完成。
10:43 测试发现：页面预览 668 元，订单接口仍返回 718 元。
10:51 退款页面也引用 legacyPrice.js，退款金额少了 50 元。
```

缺失信号：当前生效入口、前后端事实源、共享消费者、契约测试和结算验收。

## 轨迹 B：重试把最后一件库存扣了两次

```text
14:20 checkout-781 请求预占 SKU-RED-CHAIR x1。
14:20 服务完成预占，但响应在网络中断。
14:21 客户端使用新的 requestId 重试。
14:21 服务再次预占成功。
14:24 支付系统只确认一笔订单。
14:35 运营发现可售库存从 2 变成 0，预占记录有两条。
```

缺失信号：稳定幂等键、请求语义校验、重复结果缓存、预占状态查询和异常告警。

## 轨迹 C：修好了功能，破坏了边界

```text
09:10 智能体发现前端与后端价格结构不同。
09:18 为省时间，直接让 storefront 引用 services/commerce-api/src 下的 JSON 文件。
09:20 本地页面可以运行。
09:27 CI 在 Windows runner 上因路径和编码差异失败。
09:40 架构检查发现浏览器包依赖服务端源码。
```

缺失信号：依赖方向、跨平台命令、结构测试和修改前门禁。

## 轨迹 D：会话恢复后重复做了一遍

```text
17:50 智能体完成任务契约和两项测试，尚未修改实现。
18:00 会话因上下文上限压缩。
18:05 新一轮只看到原始需求，没有看到已确认的叠加规则。
18:20 智能体重新设计规则，并把“特价商品”解释成完全不能参加任何优惠。
```

缺失信号：持久计划、已确认决策、当前进度、未解决问题和恢复入口。
