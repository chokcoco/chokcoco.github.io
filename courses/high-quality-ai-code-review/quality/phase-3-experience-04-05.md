# 第三阶段产物与体验审校：04–05

- 课程：如何实现高质量的 AI Code Review
- 审校范围：第 4–5 章、课程首页、术语表与 Markdown 合集
- 审校日期：2026-07-15
- 第一阶段内容哈希：`40eadf2299a95596918dfcb9045854601386c29b8d3e165dc00c8165a5fde0a4`
- 第二阶段决定：当前批次 `ready`，整课 `manual_review`
- 本阶段决定：`manual_review`

## 浏览器验证状态

按 Browser skill 尝试连接应用内浏览器时，运行时初始化失败：

```text
Cannot redefine property: process
```

因此本轮没有改用独立 Playwright 或其他浏览器控制方式绕过限制。下面的检查属于构建产物与静态结构替代检查，不能等价替代真实浏览器视觉验收。

## 已完成的替代检查

### 页面可达性

在本地 HTTP 服务中抽查以下入口，均返回 HTTP 200：

- 课程首页
- 第 4 章《低采纳率不是模型笨：先把拒绝原因诊断清楚》
- 第 5 章《把采纳率做成工程闭环：从规则到反馈控制》
- 术语表
- 第 1–5 章 Markdown 合集

### HTML 基础结构

第 4、5 章均通过以下静态检查：

- 存在 `<!doctype html>`、`lang="zh-CN"` 与 viewport 声明。
- 每页只有一个 H1。
- 目录链接均能解析到页面中的唯一锚点。
- 本地资源与章节导航路径可解析。

### 长章节结构

| 检查项 | 第 4 章 | 第 5 章 |
| --- | ---: | ---: |
| 目录锚点 | 33，缺失 0 | 46，缺失 0 |
| Mermaid 图 | 5 | 6 |
| 表格 | 4 | 8 |
| 代码复制按钮 | 2 | 6 |

CSS 静态复核确认存在：

- 900px 与 620px 两档响应式规则；
- 宽表横向滚动处理；
- 桌面端 sticky 目录；
- 打印样式。

## 尚未验证的体验项

由于应用内浏览器未能初始化，下列项目必须保留给人工浏览器复核：

1. 11 个 Mermaid 代码块能否全部渲染为 SVG，节点换行和连线是否溢出。
2. 第 5 章 46 项目录在窄屏上的密度、折叠与滚动体验。
3. 8 个宽表在 620px 以下是否可横向滚动，首列和长代码是否保持可读。
4. 8 个复制按钮的 Clipboard 交互、成功反馈与权限失败表现。
5. sticky 目录、滚动定位、代码高亮和打印分页的真实视觉效果。

## 决定

静态结构、导航和本地可达性检查没有发现 blocker 或 major，且不需要修改学生正文。由于真实 Mermaid 渲染、移动端长目录、宽表滚动和 Clipboard 交互尚未完成浏览器验证，本阶段以及整课发布状态保持 `manual_review`。
